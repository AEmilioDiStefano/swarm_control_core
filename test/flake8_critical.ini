[flake8]
# Keep the CI gate focused on runtime-breaking issues (syntax errors and
# undefined names, e.g. the historical HAS_FLEET_ACTION NameError) while
# broader style debt is addressed incrementally.
select = E9,F63,F7,F82
jobs = 1
